# Используем легковесный образ Node.js
FROM node:18-alpine

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем файлы с зависимостями
COPY package.json ./

# Устанавливаем зависимости
RUN npm install

# Копируем все остальные файлы фронтенда
COPY . .

# Открываем порт, на котором работает Vite/React dev server
EXPOSE 3000

# Команда для запуска сервера разработки
CMD ["npm", "run", "dev"]